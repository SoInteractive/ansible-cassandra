---
ansible:
  playbook: tests/playbook.yml
#  verbose: vvv
driver:
  name: docker
verifier:
  name: testinfra
docker:
  containers:
  - name: cassandra-node01
    ansible_groups:
      - cluster
    image: jpetazzo/dind
    image_version: latest
    privileged: True
  - name: cassandra-node02
    ansible_groups:
      - cluster
    image: jpetazzo/dind
    image_version: latest
    privileged: True
  - name: cassandra-node03
    ansible_groups:
      - cluster
    image: jpetazzo/dind
    image_version: latest
    privileged: True
  - name: cassandra-single
    ansible_groups:
      - single-container
    image: jpetazzo/dind
    image_version: latest
    privileged: True