---
- hosts: cassandra
  tasks:
  - name: Install dependencies
    package:
      name: python-pip
      state: present

  - name: Install pip dependencies
    pip:
      name: docker-py

- hosts: cassandra
  roles:
    - cassandra